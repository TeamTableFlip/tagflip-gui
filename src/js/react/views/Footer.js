import React, {Component} from "react";

/**
 * A React view representing the footer.
 */
class Footer extends Component {
    /**
     * Render the footer.
     * @returns {*} The footer to be rendered.
     */
    render() {
        return (
            <footer id="footer">
                Footer!
            </footer>
        );
    }
}

export default Footer;